This folder contains follow-along code executed from the Coding Adict freecodecamp node.js course.

For any code to be run, simply copy into app.js and run from there. File paths are setup to work from there (app.js). If you decide to run from other files, then be sure to update paths accordingly. 
